version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir www
        - mv * www/ || true
  artifacts:
    baseDirectory: www
    files:
      - '**/*'
  cache:
    paths: []
